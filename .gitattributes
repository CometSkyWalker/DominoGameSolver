# Единые окончания строк в репозитории: в git всё хранится с LF,
# в рабочей копии Windows получает привычные CRLF автоматически.
* text=auto eol=lf

# Двоичные файлы не трогать.
*.png binary
*.pdf binary
